import {describe,it,expect} from "vitest";import {IntelligenceSchema} from "../lib/intelligence";describe("intelligence schema",()=>{it("requires pending review",()=>{const x=IntelligenceSchema.parse({summary:"s",decisions:[],risks:[],actionItems:[{task:"Send notes",owner:null,dueDate:null,sourceSegmentIds:["x"],reviewStatus:"pending"}]});expect(x.actionItems[0].reviewStatus).toBe("pending")})});
