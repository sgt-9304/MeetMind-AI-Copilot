# Contributing
Use synthetic or properly consented audio only. Open an issue, add tests, and run `npm run test` plus `npm run build`. Never commit recordings, transcripts, credentials, customer information, employer/client data, or covert-recording features.
